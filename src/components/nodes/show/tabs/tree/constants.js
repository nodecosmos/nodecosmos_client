export const MARGIN_LEFT = 20; // move children's edge left from where current node's X edge ends
export const NODE_BUTTON_HEIGHT = 28.5; // height of node button
export const MARGIN_TOP = NODE_BUTTON_HEIGHT / 2.0 + 0.5; // move children's edge down from current node's button y
export const EDGE_LENGTH = 30; // length of edge (link)
