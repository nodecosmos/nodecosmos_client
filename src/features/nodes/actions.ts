import { actions } from './nodesSlice';

export const {
    buildTmpNode,
    updateState,
    deleteFromState,
    select,
    search,
    setDragAndDrop,
    setNodePaneContent,
    setActionInProgress,
    clearJustCreatedNode,
} = actions;
