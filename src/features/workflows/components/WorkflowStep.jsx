import React from 'react';
import PropTypes from 'prop-types';

export default function WorkflowStep({ wfStep }) {
  return (
    <g />
  );
}

WorkflowStep.propTypes = {
  wfStep: PropTypes.object.isRequired,
};
